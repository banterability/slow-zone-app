<li class="cta-train cta-{{route.class}}">
  <figure>
    {{ prediction.arrivalMinutes }}m
  </figure>
  <figcaption>
    <div class="prediction">{{ prediction.arrivalString }} {{#status.scheduled}}(scheduled){{/status.scheduled}} towards {{destination.name}}</div>
    <div class="prediction-age">predicted {{ prediction.predictionAge }}s ago</div>
  </figcaption>
</li>
