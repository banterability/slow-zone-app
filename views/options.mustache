{{#predictions}}
    {{#next}}
    <h3>{{heading}} @ {{prediction.stop.name}}</h3>
    <p>{{upcoming}}</p>
    <ul class="details">
      <li class="next">{{ prediction.arrivalTime }} towards {{train.destination.name}}</li>
      {{#future}}
        <li>{{ prediction.arrivalTime }} towards {{train.destination.name}}</li>
      </li>
      {{/future}}
    </ul>
    {{/next}}
    {{^next}}
      <p>No trains approaching.</p>
    {{/next}}
{{/predictions}}