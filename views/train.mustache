<li class="cta-train cta-{{route.class}}">
  <span class="bubble">{{ prediction.arrivalMinutes }}m</span>
  {{ prediction.arrivalString }} {{#status.scheduled}}(scheduled){{/status.scheduled}} towards {{destination.name}}
</li>
